[workspace]
resolver = "2"
members = [".", "crates/apply_bg", "crates/mask_tool", "crates/score_video"]

[workspace.package]
version = "1.3.5"
edition = "2021"
license = "MIT"

[package]
name = "score_sync"
version.workspace = true
edition.workspace = true
description = "方便曲谱同步一条龙制作的 Rust GPUI 工具"
license.workspace = true

[dependencies]
async-channel = "2"
clap = { version = "4", features = ["derive"] }
gpui = { version = "0.2.2", default-features = false, features = ["windows-manifest", "font-kit"] }
image = { version = "0.25", default-features = false, features = ["png", "jpeg", "tiff", "bmp", "webp"] }
mask_tool = { path = "crates/mask_tool" }
apply_bg = { path = "crates/apply_bg" }
pdfium-render = { version = "0.8", default-features = false, features = ["pdfium_6406", "image", "thread_safe"] }
rfd = "0.15"
score_video = { path = "crates/score_video" }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
smallvec = "1"
uuid = { version = "1", features = ["v4"] }
zip = { version = "2", default-features = false, features = ["deflate"] }
ureq = "2"

[target.'cfg(windows)'.build-dependencies]
embed-resource = "3"

# 日常 `cargo run -r`: 够快, 链接不再拖到数分钟.
[profile.release]
lto = false
codegen-units = 16
opt-level = 3
strip = true

# 最终发包再开满优化: cargo build --profile release-max
[profile.release-max]
inherits = "release"
lto = true
codegen-units = 1
